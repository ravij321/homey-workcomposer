import {Router} from'express';import{requireAuth}from'../auth.js';import{requireRole}from'../roles.js';import{query}from'../db.js';const router=Router();router.get('/',requireAuth,requireRole('admin'),async(req,res)=>{try{const r=await query(`SELECT a.id,a.action,a.resource_type,a.resource_id,a.metadata,a.created_at,u.name AS actor_name,u.email AS actor_email FROM audit_events a LEFT JOIN users u ON u.id=a.actor_user_id ORDER BY a.created_at DESC LIMIT 250`);res.json({events:r.rows})}catch(e){res.status(500).json({error:'Unable to load audit history'})}});export default router;
