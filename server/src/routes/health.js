import {Router} from'express';import{query}from'../db.js';const router=Router();router.get('/',async(req,res)=>{const checks={api:true,database:false,scalefusion:Boolean(process.env.SCALEFUSION_API_TOKEN)};try{await query('SELECT 1');checks.database=true}catch{}res.status(checks.database?200:503).json({ok:checks.database,...checks,timestamp:new Date().toISOString()})});export default router;
