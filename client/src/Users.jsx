import React,{useEffect,useState}from'react';const API=import.meta.env.VITE_API_URL||'http://localhost:4000';export default function Users({token}){const[data,setData]=useState([]);useEffect(()=>{fetch(`${API}/api/users`,{headers:token?{Authorization:`Bearer ${token}`}:{}}).then(r=>r.json()).then(x=>setData(x.users||[])).catch(()=>setData([]))},[token]);return <section className="pagePanel"><div className="pageTitle"><div><h2>Users</h2><p>People, teams and current work activity</p></div><button>＋ Invite user</button></div><div className="table"><div className="thead"><span>User</span><span>Department</span><span>Activity</span><span>Device</span><span>Status</span></div>{data.map(u=><div className="trow" key={u.id}><span><b>{u.name}</b><small>{u.email}</small></span><span>{u.department}</span><span>{u.hours}h</span><span>{u.device}</span><span className="badge good">{u.status}</span></div>)}</div></section>}